# UPGRADE_NOTES —— 已知局限与升级路线

> 这份文件回答一个问题：**"下次想让引擎变强，该动哪里？"**
>
> **M1–M9 已全部完成**（212 用例 / 80186 断言全过）。下面按里程碑记录每个模块
> 刻意做了什么简化、什么时候会露馅、以及升级要动哪里。当前最该做的一条是
> **求解器孤岛**（见 M9 局限 1）。
>
> 每完成一个模块就在这里追加一节，记录三件事：
>   1. 当前实现刻意做了什么简化
>   2. 这个简化在什么情况下会露馅（症状是什么）
>   3. 升级要改哪些文件、大概多大工作量
>
> 代码里对应的位置都用 `TODO(upgrade)` 标注，可以直接 grep：
> ```
> grep -rn "TODO(upgrade)" include src
> ```

优先级说明：
- **P0** — 已经能预见到会影响目标场景（类 CS 的 FPS），迟早要做
- **P1** — 场景变复杂之后才会成为瓶颈
- **P2** — 锦上添花，或只在特定玩法下才需要

---

## M1 — 数学基础库 + core（已完成）

实现状态：`include/pe/math/*`、`include/pe/core/*`
测试：`tests/test_math.cpp`、`tests/test_core.cpp` —— 52 用例 / 293 断言全过

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | `real = float`（单精度） | 大地图（坐标绝对值超过约 5000 米）时，float 的有效位数降到厘米级，接触点抖动会肉眼可见。FPS 地图通常在 ±200 米内，暂时无影响 | P2 |
| 2 | `SlotArray` 是稀疏数组，删除后留空洞 | 大量高频创建/销毁的对象（满地弹壳、粒子）会让空洞率升高，`ForEach` 遍历浪费缓存 | P1 |
| 3 | `Quat::Integrate` 是一阶显式近似 | 单步转角接近 pi 时（极高速自旋）角度会系统性偏小。当前 1/60 步长下需要 180 rad/s 才会出问题 | P2 |
| 4 | 无 SIMD | 单线程标量运算。50–100 个动态体的目标下不是瓶颈 | P2 |
| 5 | `Transform` 不含缩放（刻意设计） | 想要"同一个碰撞体缩放成不同大小"时做不到，必须建不同参数的 Shape | 不打算改，见 `Transform.h` 顶部说明 |

### 升级方向

**(1) 换成 double 精度**
改 `pe/core/Types.h` 里的 `using real = float;` 一行即可。
前提是所有字面量都写成了 `real(1.5)` 的形式 —— 当前代码已经严格遵守，
`-Wconversion` 也在编译期把关。测试里的容差常量需要相应收紧。

**(2) SlotArray 升级为"稠密数组 + 间接表"**
文件：`include/pe/core/SlotArray.h`（对外接口完全不变，纯内部改造）
做法：`sparse[handle.index] -> dense 下标`，删除时把 dense 数组的最后一个元素
搬到空位并回填间接表。代价是删除时对象会搬家，所以 `Get()` 返回的指针
稳定性更差（当前实现里对象不会因为别人被删而搬家）。
判断时机：剖析显示 `ForEach` 成为热点，或空洞率长期超过 50%。

**(3) 四元数积分换成指数映射**
文件：`include/pe/math/Quat.h` 的 `Quat::Integrate`
把 `q + 0.5*dt*(omega⊗q)` 换成精确形式
`FromAxisAngle(omega.Normalized(), |omega|*dt) * q`，代价是一次 sin/cos。
测试 `tests/test_math.cpp` 里已有的积分用例可以直接复用（收紧容差即可）。

**(4) SIMD**
`Vec3` 当前是 12 字节。要上 SSE/NEON 需要补到 16 字节对齐（加一个 padding 分量），
这会改变 `sizeof(Vec3)`，而 `test_math.cpp` 里有 `sizeof(Vec3) == 3*sizeof(real)`
的静态断言在守着这一点 —— 那个断言就是提醒你"改这里会影响内存布局"的哨兵。
真要做的话，建议不要动 `Vec3`，而是新增一个 SoA 的批处理路径（`Vec3x4` 之类），
只在求解器的热循环里用。

### 这一层留下的、后续模块要遵守的约定

这些约定被单元测试钉死了，改动前先看对应的用例：

| 约定 | 守护它的测试 |
|---|---|
| 右手坐标系，X × Y == Z | `Vec3 点积与叉积` |
| 行优先存储，列向量右乘 `v' = M * v` | `Mat3 矩阵乘向量是列向量右乘` |
| 四元数 `q1 * q2` 表示"先做 q2 再做 q1" | `Quat 复合顺序` |
| 正角度 = 逆时针（从轴正方向朝原点看） | `Quat 绕轴旋转方向符合右手定则` |
| 点会被平移，方向不会 | `Transform 点与方向的变换不同` |
| 导出给 OpenGL 的是列优先，平移在下标 12/13/14 | `Mat4 列优先导出符合 OpenGL 布局` |
| 数学类型是平凡可复制的 POD | `数学类型保持平凡可复制` |
| 退化输入（零向量/奇异矩阵）返回安全值而非 NaN | 各 `[degenerate]` 标签的用例 |

**特别提醒**：如果你现有的渲染层用的是列优先 + 行向量左乘，
现在改 `Mat3.h` 顶部的约定注释和 `Mat4::ToColumnMajorArray()` 的代价，
远低于等到 M9 整合 Demo 时再改。

---

## M2 — 形状 + AABB + 射线求交（已完成）

实现状态：`include/pe/collision/{AABB,Ray,Shape,GeometryUtil,RayCast}.h`、
`src/collision/{Shape,RayCast}.cpp`
测试：`tests/test_collision_shapes.cpp`、`tests/test_raycast.cpp`
累计 **101 用例 / 10296 断言全过**（含 M1）

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | ~~只有离散射线，没有形状扫掠 / CCD~~ | **已在 M8/M9 关闭**：`ShapeCast`（保守推进）+ `PhysicsWorld` 里的 CCD。实测 300 m/s 的球撞 20 厘米厚的墙能被拦住 | ~~P0~~ 已解决 |
| 2 | 没有 ConvexHull 形状 | 想用任意凸多面体（不规则掩体、载具）做碰撞体时做不到 | P1 |
| 3 | 射线起点在内部时法线取 -direction，不是真实的最近表面法线 | 需要"从内部把物体推出去"的场合方向会不对。当前用途（命中判定）不需要 | P2 |
| 4 | `RaycastCapsule` 每次都做一遍 Transform 的逆变换 | 对同一个胶囊打大量射线时（比如 AI 视线批量检测）有重复开销 | P2 |
| 5 | 没有射线 vs 三角网格 | 想直接用美术导出的地图网格做碰撞时。当前方案是用 AABB/OBB 拼出碰撞体 | P1 |

### 升级方向

**(1) CCD / ShapeCast —— 优先级最高**
文件：新增 `include/pe/collision/ShapeCast.h`，接口占位注释已写在 `RayCast.h` 顶部。
原理：形状扫掠本质上是"对闵可夫斯基和做射线求交"。
- **球扫掠**最简单：球沿位移扫过去撞静态形状，等价于把静态形状**外扩球半径**
  之后打一条射线。所以 球-vs-球 变成 射线-vs-大球，球-vs-盒 变成
  射线-vs-圆角盒。这一条就够覆盖抛射物了。
- **任意凸形状**需要 M5 的 GJK 配合 conservative advancement（保守推进）：
  反复求当前最近距离，按相对速度推进一个安全步长，直到距离小于容差。

判断时机：做出手雷/火箭之后，第一次发现它穿墙时。在那之前不用做。

**(2) 加入 ConvexHull**
等 M5 的 GJK/EPA 完成后：
1. 在 `ShapeType` 里加 `ConvexHull`，编译器（`-Wswitch`）会列出所有需要补的 switch
2. `Shape` 的 union 里加 `struct { uint32_t hullIndex; } hull;`
3. World 持有一个顶点池，`hullIndex` 索引进去
4. `LocalAABB` 由顶点集算；`Volume` 用四面体分解求和；射线求交走 GJK 的扫掠路径

**(3) 三角网格碰撞体（静态几何）**
如果想直接吃美术导出的地图网格，需要新增一种"非凸的静态形状"：
BVH 索引的三角面片集合。它只能是静态的（三角网格没有内部/外部之分，
无法定义穿透深度）。这是个独立的中等规模模块，不在当前里程碑里。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| `Ray::direction` 是单位向量，`distance` 的单位是米 | `Ray 构造函数归一化方向` |
| 起点在物体内部 -> 命中、distance = 0、法线 = -direction | 各形状的 `起点在内部` 用例 |
| 胶囊 `halfHeight` 不含端盖（总高 = 2*(halfHeight+radius)） | `胶囊的 halfHeight 不含端盖` |
| AABB 相交判定用闭区间（贴面算相交，宽相位不可漏报） | `AABB 相交判定用闭区间` |
| `ComputeWorldAABB` 必须紧致，不能退化成包围球 | `世界 AABB：盒子旋转 45 度的紧致性` |
| 形状局部原点 = 几何中心（也是 M6 的质心） | `世界 AABB：未旋转时等于局部 AABB 加平移` |

---

## M3 — 宽相位 Uniform Grid（已完成）

实现状态：`include/pe/collision/{BroadPhase,UniformGrid}.h`、`src/collision/UniformGrid.cpp`
测试：`tests/test_broadphase.cpp`
累计 **116 用例 / 11865 断言全过**（含 M1–M2）

实测（2000 静态 + 100 动态，`cellSize = 2`）：每帧 100 次 `Update` + 一次
`QueryPairs` + 一条 `QueryRay` 合计 **0.075 ms**，同场景暴力 O(n²) 是 **3.96 ms**。

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | ~~`kAabbMargin` 太小，fat AABB 基本不起作用~~ | **已在 M9 关闭**：`Update()` 加了单独的预测量参数，World 按速度传入。重建率从 94% 降到个位数。注意"把 AABB 扫掠好再传进来"（下面 (1) 原本推荐的做法）是**无效**的，理由见 M9 那一节 | ~~P0~~ 已解决 |
| 2 | `cellSize` 是全局固定值，物体尺寸跨度大时必然两头不讨好 | 场景里同时有 0.2 米的手雷和 20 米的集装箱。当前实测「8.4 格/proxy」已经偏高 | P1 |
| 3 | `Bounds()` 只增不减 | 有物体飞到很远处又被销毁之后，包围盒不会缩回来，射线遍历会在空区域多走一段。只影响速度不影响结果 | P2 |
| 4 | 超大体 vs 全体是 O(超大体数 × proxy 总数) 的线性扫 | 超大体数量上双位数时。一张地图正常只有个位数 | P2 |
| 5 | `isStatic` 注册后不能改 | 想让一堵墙从静态变成可破坏的动态体时，必须 `Remove` + `Insert`，proxy id 会变 | P2 |
| 6 | 没有 `QueryShape`（用形状而不是 AABB 做重叠查询） | M8 的触发器和角色控制器会想要「胶囊体重叠查询」。当前只能先用 AABB 粗筛再自己精筛 | P1 |
| 7 | 单线程 | 目标规模下不是瓶颈。`QueryPairs` 按格子分块天然可并行，但输出要合并 | P2 |

### 升级方向

**(1) 速度感知的 fat AABB —— 优先级最高，而且很便宜**

这是实测暴露出来的问题：以 5 m/s 移动的物体在 1/60 秒里走 0.083 米，
而 `kAabbMargin` 只有 0.05 米，于是几乎每帧都要重建格子登记 —— fat AABB 这个
机制等于没开。业界标准做法是把 margin 拆成两部分：

```
fatAabb = tightAabb.Expanded(kAabbMargin) 然后再 Sweep(velocity * dt * k)
```

`AABB.h` 里的 `Sweep(box, displacement)` 就是为这个准备的，已经写好没人用。
`k` 取 2~4（预测未来几帧）。代价是 AABB 变大、候选对变多，所以要连带把
`kAabbMargin` 调小一点补回来。

改动位置：`UniformGrid::Update` 加一个可选的 `velocity` 参数，或者更干净的做法是
让上层（`PhysicsWorld`）自己算好扫掠过的 AABB 再传进来 —— 宽相位不该知道速度的
存在（见 ARCHITECTURE.md §2）。**推荐后者**。

判断时机：现在就可以做，M6 有了刚体速度之后立刻能接上。

**(2) 换成 BVH / 动态 AABB 树**

文件：新增 `include/pe/collision/DynamicBVH.h`，实现同一个 `IBroadPhase`，
`PhysicsWorld` 一行都不用改 —— 这正是 M3 把接口和实现拆开的目的。
`AABB::SurfaceArea()` 已经写好了，就是给 BVH 的 SAH（表面积启发式）建树用的。

判断时机：出现「物体尺寸跨一个数量级以上」或者「地图大部分是空的」的场景。
均匀网格的失效信号很明确 —— 把 `ComputeStats()` 打出来看：
`avgCellsPerProxy` 长期大于 10，或者 `maxProxiesPerCell` 是 `avgProxiesPerCell`
的几十倍，就说明该换了。

**(3) 多级网格（分层均匀网格）**

比上 BVH 便宜的中间方案：按物体尺寸分成 2~3 档，每档一张自己的网格
（`cellSize` 逐档翻倍），查询时逐档查。能吃掉局限 2 的大部分，
而且代码结构和现在几乎一样。

**(4) `QueryShape` / 扫掠查询**

M8 的角色控制器需要「胶囊沿位移扫过去会撞到谁」。宽相位这一侧只要
`QueryAABB(扫掠后的 AABB)` 就够了，精确判定是窄相位的事 ——
所以这一条其实不用改宽相位，只是提醒一下别在这里重复造轮子。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| 配对结果 == 对所有 proxy 两两跑 `BroadPhaseShouldCollide`，不多不少 | `与暴力 O(n^2) 对拍：1000 个随机物体` |
| 同一对候选**只吐一次**，哪怕两个物体共占几十个格子 | `跨多个格子的物体只被吐出一次` |
| 过滤规则只有 `BroadPhaseShouldCollide` 一处，任何实现都必须复用 | 对拍用例（暴力参考实现直接调用它） |
| `cellSize` 只影响速度、绝不影响结果 | `极端 cellSize 下结果不变` |
| 配对用 fat AABB（比紧致 AABB 早一点产生候选对），这是 warm starting 的前提 | `fat AABB 让小幅移动不动哈希表` |
| `QueryAABB` / `QueryRay` **不做**层过滤（查询发起方没有层） | `AABB 查询不做层过滤` |
| 静态-静态、双方休眠的对一定不出现 | `过滤规则` 各 SECTION |
| 坐标算出 inf / NaN 时降级成超大体，绝不把哈希表撑爆 | `坐标爆炸时不会把网格撑爆` |
| 物体注销后不留空格子在哈希表里 | `增删改之后仍与暴力一致` 末尾的格子数断言 |

## M4 — 窄相位解析解（已完成）

实现状态：`include/pe/collision/{Manifold,NarrowPhase}.h`、`src/collision/NarrowPhase.cpp`
（另外给 `GeometryUtil.h` 补了 `ClosestPointsSegmentSegment` 与 `ClosestPointSegmentAABB`）
测试：`tests/test_narrowphase.cpp`
累计 **127 用例 / 29295 断言全过**（含 M1–M3）

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | 胶囊 vs 盒 / 胶囊 vs 胶囊只给**一个**接触点 | 胶囊**躺平**压在面上时（ragdoll 倒地、圆柱形道具滚落）。一个点撑不住，物体会绕接触点摇晃。角色站立（底端盖顶着地面）是真正的单点接触，不受影响 | P1 |
| 2 | 胶囊 vs 盒在"轴线整个插进盒子里"时是**采样近似** | 深度穿透（生成物体时嵌在墙里、求解器发散）。此时穿透深度和法线只是大致方向，MTV 不精确 —— 测试里那条"推开后必须分离"的不变量特意跳过了这种情形 | P2 |
| 3 | 没有 ConvexHull | 不规则掩体、载具。等 M5 | P1 |
| 4 | ~~流形每帧从零重算，没有跨帧持久化~~ | **已在 M7 关闭**：`ContactCache` 按 `(pairKey, featureId)` 继承累积冲量。实测关掉之后 10 层箱塔 5 秒内整个塌掉 | ~~P0~~ 已解决 |
| 5 | 盒-盒不满足 A/B 交换对称性 | 只影响接触点的分布，法线与穿透深度仍然对称。刻意的取舍，见下 | 不打算改 |
| 6 | 盒-盒的 4 点精简用的是"最深点 + 最远点 + 基线两侧各一"，不是严格的最大面积四边形 | 裁剪结果超过 4 个点时（两个盒子斜着大面积贴合）。绝大多数构型裁出来正好 4 个点，精简根本不触发 | P2 |

### 升级方向

**(1) 胶囊的多点流形**

胶囊躺平在面上时，接触区域是一条**线段**而不是一个点。做法：算出接触法线之后，
判断胶囊轴线与接触平面是否近似平行（`|Dot(轴向, 法线)| < 0.05` 之类），
是的话就把轴线在接触面上的投影裁到面内，取两个端点各生成一个接触点。
文件：`src/collision/NarrowPhase.cpp` 的 `CollideCapsuleBox` / `CollideCapsuleCapsule`。

判断时机：做出 ragdoll、或者场景里出现横放的圆柱形物件时。
**FPS 的核心玩法用不到** —— 站立的角色胶囊对地面本来就是单点接触。

**(2) 深度穿透时的精确 MTV**

现在那四个采样点（两端 + 中点 + 最近点参数）是权宜之计。要精确的话，
"线段上到盒表面距离最大的点"是一个凹函数在区间上求最大值，
可以按 6 个面的线性函数求上包络的折点解出来，大约二三十行。
或者干脆等 M5 的 GJK/EPA —— 深度穿透正是 EPA 的主场。

**(3) 接触流形的跨帧持久化（M7 一定要做）**

窄相位这一侧已经把 `featureId` 生成好了，剩下的全在求解器：
按 `(colliderA, colliderB)` 缓存上一帧的流形，新旧接触点按 `featureId` 配对，
把累积冲量继承过来。少了它，箱子堆会肉眼可见地下陷再弹回。

**(4) 快路径与慢路径的对拍（M5 的验收条件）**

M5 的 GJK/EPA 做完之后，盒-盒会有两条独立的实现路径。
拿同一对盒子跑两条路径、要求法线和穿透深度一致，是检验 GJK/EPA 的最好办法
—— 因为解析解这一侧已经被本轮的构造用例和随机不变量钉死了。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| `normal` 是单位向量，方向**始终由 A 指向 B** | `CheckManifoldConventions` + 各构造用例 |
| 沿 `normal` 把 B 推开 `penetration` 恰好分离（MTV 语义） | `沿法线推开穿透深度之后必须分离` |
| 接触点位置 = 两个见证点的**中点** | 各构造用例里写死的坐标 |
| `Collide(A,B)` 与 `Collide(B,A)`：法线相反、深度相同、接触点相同（盒-盒只保证前两条） | `交换顺序的对称性` |
| 返回 `false` 时流形已被 `Clear()`，读到的是干净的空流形 | `分离时不报接触` 各 SECTION |
| 表面相距 `kSpeculativeMargin` 以内就报接触，此时 `penetration` 为负 | `恰好相切：推测性接触，深度为 0` |
| 盒-盒面接触必须给出 4 个点（单点撑不住堆叠） | `面对面：应该给出 4 个等深的接触点` |
| `Overlap()` 与 `Collide()` 的判定必须一致 | `Overlap 与 Collide 一致` |
| 退化输入（零半径、薄片盒、完全重合）返回安全值而非 NaN | 各 `[degenerate]` 用例 |

---

## M5 — GJK + EPA（已完成）

实现状态：`include/pe/collision/{GJK,EPA}.h`、`src/collision/{GJK,EPA}.cpp`，
通用入口 `CollideConvex()` / `ConvexDistance()` 在 `NarrowPhase.h`
测试：`tests/test_gjk.cpp`
累计 **135 用例 / 63492 断言全过**（含 M1–M4）

实测（2 万对随机旋转的盒子）：SAT 解析解 0.77 us/对，GJK+EPA 1.97 us/对（慢 2.6 倍）；
GJK 平均 3.5 轮收敛，最多 8 轮。

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | `ConvexHull` 仍未进 `ShapeType` —— 缺的不是算法而是**顶点池的归属** | 想用不规则掩体、载具做碰撞体时。见下面 (1)，M8 有了 World 就能做 | P1 |
| 2 | 通用路径只给**一个**接触点 | 用它做堆叠时。目前它只用于交叉验证，`Collide()` 默认走解析解，所以还没有影响 | P1（和 (1) 是同一件事的两半） |
| 3 | EPA 的见证点可能沿切向偏一点 | 接触点位置的精度。原因：EPA 把原点投影到最近的**三角面**上，投影点可能落在该三角形外（一个平面常被切成好几个三角形），夹回去就偏了。法线和深度不受影响 —— 它们只取决于面所在的平面 | P2 |
| 4 | EPA 的多面体是线性扫描找最近面，O(面数) | 面数上百时。当前迭代上限 48，实测远用不到 | P2 |
| 5 | 固定容量（64 顶点 / 128 面 / 64 条地平线边）用满时返回当前最好的面 | 极端退化的输入。是刻意的降级策略：精度差一点好过失败 | 不打算改 |
| 6 | 单精度下 EPA 对"薄片形状"精度有限 | 半尺寸接近 0 的盒子。有用例守着不产生 NaN，但深度的有效位数会掉 | P2 |

### 升级方向

**(1) 加入 ConvexHull —— 现在只差存储**

算法在 M5 已经就位，`ConvexProxy` 也是照着这个需求设计的（它是瞬时的栈上对象，
所以可以放裸指针，而 `Shape` 继续保持不含指针的 POD）。步骤：

1. `ShapeType` 里加 `ConvexHull`，`-Wswitch` 会把所有需要补的分派点一个不漏地列出来
2. `Shape` 的 union 里加 `struct { uint32_t hullIndex; } hull;`
3. `PhysicsWorld`（M8）持有顶点池，查询开始时把 hullIndex 解析成指针填进 `ConvexProxy`
4. `ConvexProxy::SupportCore` 加一个分支：遍历顶点取 `Dot(dir, v)` 最大的那个
   （顶点数上百时可以预计算邻接表走爬山法，但先用线性扫描）
5. `LocalAABB` 由顶点集算；`Volume` 用四面体分解求和；`CoreRadius` 返回 0

判断时机：M8 做出 World 之后立刻可以做。

**(2) 通用路径的多点流形**

EPA 只给一个接触点。要生成完整的面接触，需要拿 EPA 的法线在两个形状上各找一个
"最迎着法线"的面，再做和盒-盒一样的 Sutherland-Hodgman 裁剪
（`src/collision/NarrowPhase.cpp` 里那套裁剪代码可以直接复用）。
前提是形状能枚举自己的面 —— 所以这一条和 (1) 是同一件事的两半。

**(3) EPA 的最近面查询换成优先队列**

现在每轮线性扫描所有面找最近的那个。面数上百时可以换成按 distance 排序的小顶堆
（Bullet 的做法）。当前规模下线性扫描更快（缓存友好、没有堆维护开销），
真要换之前先剖析。

**(4) 用 GJK 做 ShapeCast / CCD**

`ConvexDistance()` 已经能给出任意两个凸形状的距离，这正是 **conservative advancement**
（保守推进）需要的：反复"求当前距离 -> 按相对速度推进一个安全步长"，
直到距离小于容差。这是 M2 的 UPGRADE_NOTES 里那条 P0（抛射物穿墙）的通用解法，
比只做球扫掠更彻底。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| `SupportCore` 返回的是**核**（不含半径），半径由 `CoreRadius()` 单独给 | `GJK 支撑函数` |
| GJK 与解析解算的是同一个 MTV，盒-盒的差值有解析上界（面/边偏置） | `与解析解对拍：盒vs盒` |
| EPA 给出的深度是**真正的最小值**（不带任何偏置） | 同上的 `depthConvex <= depthAnalytic` |
| 闵可夫斯基差退化成点/线段/平面时不误报、不产生 NaN，核穿透为 0 | `退化输入` 各 SECTION |
| GJK 在核都是多面体时有限步收敛（实测最多 8 轮） | `GJK 收敛速度在合理范围内` |
| `ConvexDistance` 返回的是**表面**距离（已减掉两个半径），相交时为 0 | `GJK 距离查询` 各 SECTION |

## M6 — 刚体动力学（已完成）

实现状态：`include/pe/dynamics/{RigidBody,MassProperties,Material,Integrator}.h`、
`src/dynamics/{MassProperties,Integrator}.cpp`
测试：`tests/test_dynamics.cpp`
累计 **151 用例 / 63620 断言全过**（含 M1–M5）

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | 一个刚体只能有一个碰撞体，质心恒在形状原点 | 想做"箱子上焊一根杆"这种复合形状时。`MassProperties::centerOfMass` 和 `TranslateInertia()` 已经为此备好，缺的是 M8 的 Collider 列表 | P1 |
| 2 | 陀螺项限幅之后不再严格守恒角动量 | 极高速自旋（每帧转角接近 pi）。限幅是为了防发散，是刻意的取舍 | P2 |
| 3 | 每帧一次 3x3 求逆算 `I_world`（陀螺项要用 `L = I*w`，手上只有逆） | 上千个动态体时。100 个体一帧才三千次浮点运算，当前完全不是瓶颈 | P2 |
| 4 | 四元数积分是一阶近似（继承自 M1） | 单步转角接近 pi 时角度会系统性偏小。1/60 步长下需要 180 rad/s | P2 |
| 5 | 休眠是逐个刚体判断的，没有"孤岛"概念 | 一摞箱子里最上面那个还在微动，下面的会各自睡着又被唤醒。真正的做法是把接触图连通分量整体休眠 | P1（M7 做求解器孤岛时一起做） |
| 6 | 没有速度上限 | 数值爆炸时速度会冲到 1e30，宽相位那边有兜底（转成超大体），但 clamp 一下更省心 | P2 |

### 升级方向

**(1) 复合形状的质量属性**

M8 让一个刚体挂多个 Collider 之后：对每个 Collider 算 `MassProperties`，
按质量加权求出合成质心，再用 `TranslateInertia()` 把每个惯量张量搬到合成质心上相加。
两个函数都已经写好并有测试守着，缺的只是 Collider 列表。

注意合成之后**惯量张量不再是对角阵**（惯量积不为零），
`invInertiaLocal` 已经是 `Mat3` 所以接口不用改，但 `Mat3::Inverse()` 会走一般路径。

**(2) 休眠孤岛（sleep island）**

现在每个刚体独立判断休眠。正确的做法是按接触关系求连通分量，
整个孤岛一起睡、一起醒 —— 否则一摞箱子会出现"下面睡了上面还在动，
然后下面被唤醒"的抖动循环。
需要 M7 的接触图，所以放在那时一起做。

**(3) 速度上限**

`kMaxLinearVelocity` / `kMaxAngularVelocity` 两个常量加在 `Types.h`，
在 `IntegrateVelocity` 末尾夹一下。属于"不要求正确但要求不爆炸"的防御措施。

**(4) 换 double 之后收紧容差**

`tests/test_dynamics.cpp` 里角动量守恒那条用的是 5% 的容差，
那是单精度 + 一阶积分的现实。换成 double 之后应该能收到千分之一。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| `invMass = 0` 表示无穷大质量，冲量公式自动失效，求解器里不需要 `if (isStatic)` | `invMass=0 让冲量自动失效，不需要任何特判` |
| 静态与运动学的区别**只在于积不积分位置**，不在于质量 | `运动学体会被积分位置，静态体不会` |
| 速度积分与位置积分是两个函数，求解器夹在中间（半隐式的定义） | `半隐式欧拉的能量有界` |
| 阻尼与步长无关（`pow` 而非线性） | `阻尼是指数衰减且与步长无关` |
| 改写 `rotation` 之后必须调 `UpdateWorldInertia` | `IntegratePosition 会自动刷新世界惯量` |
| 外力每帧必须 `ClearForces`，否则无限累积 | `不清零的话力会无限累积` |
| 摩擦取几何平均、恢复取最大值，且与顺序无关 | `材质组合规则` |
| 惯量张量：每根轴由**另外两根**轴的尺寸决定 | `长方体：每根轴的惯量由另外两根轴的尺寸决定` + 蒙特卡洛对拍 |

---

## M7 — 顺序冲量求解器（已完成）

实现状态：`include/pe/solver/{IConstraint,ContactConstraint,SequentialImpulseSolver}.h`、
`src/solver/{ContactConstraint,SequentialImpulseSolver}.cpp`
测试：`tests/test_solver.cpp`（内含一个 `MiniWorld`，是 M8 里 `PhysicsWorld::Step()` 的可执行草稿）
累计 **170 用例 / 78550 断言全过**（含 M1–M6）

实测标定（10 层箱塔，每个 10 kg，5 秒）：

| velocityIterations | 结果 |
|---|---|
| 4 | 约 7 秒时塌掉 |
| 8（默认） | 稳定，顶层横向摇晃 ±0.24 米 |
| 16 | 稳定，摇晃 ±0.10 米 |
| 32 | 稳定，摇晃 ±0.13 米（收益饱和） |

关掉 warm starting：8 轮迭代下**整座塔 5 秒内彻底塌掉**（顶层从 9.59 落到 0.65）。

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | 高塔有肉眼可见的**摇晃**（10 层 ±0.24 米 @ 8 轮） | 场景里出现 8 层以上的堆叠时。摇晃是来回晃不是单向漂移，属于 Gauss-Seidel 收敛不足 | P1 |
| 2 | 库仑摩擦是**方盒近似**：两个切向各自钳到 ±mu·Pn | 沿切向基对角线滑动时摩擦上限被放大到 sqrt(2)·mu。由于切向基由法线现算、朝向是任意的，表现为"摩擦力大小取决于切向基朝哪儿" | P1 |
| 3 | Baumgarte 位置修正会往系统里注入能量 | 深穿透被推开时。`maxLinearCorrection` 已经限住了最坏情况（实测埋进地里 1 米的箱子，推出过程峰值速度 < 6 m/s，不会弹飞） | P2 |
| 4 | 没有求解器孤岛（islands） | 场景里有多组互不相干的堆叠时，它们被当成一个整体求解，无法分别休眠、也无法分块并行 | P1 |
| 5 | 求解顺序 = 约束加入顺序，跨进程不保证一致 | 要做网络确定性同步时。宽相位遍历哈希表的顺序可能受内存布局影响 | P2（除非要做联机回滚） |
| 6 | 只有速度迭代，没有独立的位置迭代 | 极端受压场景（几十层塔、巨大质量比）。Baumgarte 通过速度偏置做位置修正，够用但不如显式位置求解干净 | P2 |
| 7 | 摩擦解在法向之前，用的是上一轮的法向冲量 | 法向冲量剧烈变化的那一帧，摩擦上限滞后一轮。已经补了"解完法向再收一次锥"，剩余误差在一轮迭代量级 | P2 |

### 升级方向

**(1) 更精确的摩擦锥**

现在两个切向各自钳到 ±mu·Pn（一个正方形），严格的库仑锥要求的是二维切向冲量的
**长度**不超过 mu·Pn（一个圆）。改法：两个切向都解完之后，把 (t0, t1) 当成一个二维
向量，长度超过 mu·Pn 就整体缩放回去。代价是要把两个方向的求解耦合起来。
位置：`src/solver/ContactConstraint.cpp` 的 `SolveVelocity`，那里已经有一处
"解完法向再收一次锥"的循环，把它从逐分量钳制改成二维缩放即可。

**(2) 求解器孤岛（islands）**

按接触关系求连通分量，把互不相干的堆叠分开求解。三个好处：
- 整岛一起休眠（解决 M6 局限 5：现在一摞箱子会出现"下面睡了上面还在动"的抖动循环）
- 每个岛的迭代次数可以按它的复杂度自适应
- 岛之间天然无数据依赖，是**唯一干净的并行切分点**

**(3) TGS Soft（Temporal Gauss-Seidel）**

比 Baumgarte 更现代的位置修正方案（PhysX 5 / Box2D v3 都换了）：把位置修正拆进
每一次迭代里做子步推进，而不是靠速度偏置一次性注入。堆叠稳定性会明显提升，
高塔的摇晃也会小很多。工作量中等 —— 求解循环要改成"每次迭代内部也推进位置"。

**(4) 松弛式位置求解（Relaxed NGS）**

比 (3) 便宜的中间方案：在速度迭代之后加一轮**只改位置、不改速度**的迭代，
直接把穿透推掉。因为不动速度，所以不会注入能量。Box2D v2 的
`SolvePositionConstraints` 就是这个。

**(5) 网络确定性**

要做联机回滚同步的话，得保证求解顺序跨进程一致。做法是在把候选对交给求解器之前
按 `(bodyIdA, bodyIdB)` 排序 —— 排序开销不大，但会破坏内存局部性，
所以做成编译期开关比较合适。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| 求解器**只改速度，不改位置**；位置由 `IntegratePosition` 负责 | 整个 `MiniWorld` 的步骤顺序 |
| 法向累积冲量恒 >= 0（接触只能推、不能拉） | `法向冲量永远非负` |
| 切向累积冲量恒在库仑锥内（每轮迭代结束时） | 同上 |
| 钳制的是**累积**冲量、施加的是差值 | `10 层盒堆 5 秒不下沉`（写错就收敛不了） |
| 同一对碰撞体在相邻帧必须给出同一个 `pairKey`，否则 warm starting 失效 | `warm starting 确实是堆叠稳定的关键` |
| 接触消失后缓存条目必须被清理 | `接触缓存会清理消失的接触` |
| 弹跳高度 = e² × 原高度 | `弹跳高度符合恢复系数` |
| 无外力时系统总动量守恒（冲量成对施加） | `自由对撞时动量守恒` |
| 静态体永远不被推动 | `静态体不会被推动` |
| 深穿透被平滑推出，不弹飞 | `深度穿透不会把物体弹飞` |

## M8 — 形状扫掠 + 角色控制器 + 触发器（已完成）

实现状态：`include/pe/collision/ShapeCast.h`、`include/pe/character/CharacterController.h`、
`include/pe/scene/{Events,TriggerSystem}.h` 及对应 `src/`
测试：`tests/test_character.cpp`
累计 **193 用例 / 80109 断言全过**（含 M1–M7）

**顺带关掉了 M2 的那条 P0**：`ShapeCast` 用保守推进实现，
实测"一帧走 100 米的抛射物 vs 20 厘米厚的墙"能被抓住，而只测两端的离散检测
（`Collide` 在起点和终点都返回 false）完全看不见这次碰撞。
之所以现在能做，是因为 M5 的 `ConvexDistance()` 提供了保守推进唯一需要的输入。

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | 扫掠**只平移不旋转** | 想扫掠一个一边转一边飞的物体时。游戏里几乎用不到（角色是竖直胶囊不转，抛射物用球近似） | P2 |
| 2 | ~~抛射物的 CCD 还没接进 `Step()`~~ | **已在 M9 关闭**：接在积分位置那一步，只对"位移超过自身包围球半径"的刚体做扫掠 | ~~P0~~ 已解决 |
| 3 | 上台阶时胶囊要"滚"过棱，不是瞬间上去 | 实测 0.3 米台阶用了约 8 帧（0.13 秒）爬上去。视觉上是平滑的，但比 AABB 角色（Quake/CS 用的）慢一点 | P2 |
| 4 | 角色推不动动态刚体 | 架构里写了"可以单向推动动态刚体"，但那需要 World 才能拿到刚体句柄，留给 M9 | P1 |
| 5 | 地面检测只有一次向下扫掠，没有射线兜底 | 站在两块地板的接缝、或者很窄的棱上时，扫掠拿到的是棱法线（不可站立），角色会误判为悬空。Unreal 的做法是补一条从胶囊中心向下的**射线**取真实地面法线 | P1 |
| 6 | 触发器差分是每帧遍历整个重叠集合 | 重叠对数上千时。当前是 O(重叠对数)，不是 O(n²)，对 FPS 的触发区数量完全够 | P2 |
| 7 | 没有"移动平台带着角色走" | 站在电梯/移动平台上时角色不会跟着动。需要记住脚下的刚体并把它的位移加到角色上 | P1 |

### 升级方向

**(1) 把 CCD 接进 Step —— M9 必做**

`ShapeCast` 已经就位，缺的是调用点：`PhysicsWorld::Step()` 在积分位置之前，
对"这一帧位移超过自身尺寸一半"的刚体做一次扫掠，命中就把位置截断到接触点。
判据用 `位移长度 > Shape::BoundingRadius()` 即可，不用给每个物体都做（贵且没必要）。

**(2) 地面检测加射线兜底**

`ICharacterWorld` 加一个 `RaycastCharacter`。`UpdateGrounded` 在扫掠拿到
不可站立的法线时，补一条从胶囊中心竖直向下的射线 —— 射线打在平面上拿到的是
真正的面法线，不受棱的干扰。这是 Unreal 的 `FindFloor` 的做法。
这条同时会让上台阶更干脆（局限 3）。

**(3) 移动平台**

`CharacterController` 记住 `groundBody`（脚下踩的是谁）。每帧先把该刚体
这一帧的位移/旋转施加到角色上，再做正常的移动。旋转平台还要把角色绕平台
中心转过去。工作量不大，但要小心"平台把角色挤进墙里"的情况。

**(4) 角色推动刚体**

角色撞到动态刚体时，按角色的质量和速度对刚体施加冲量。
注意**必须是单向的** —— 刚体不能反推角色，否则手感立刻被物理接管。
需要 World 才能从扫掠结果拿到刚体句柄，所以放在 M9。

**(5) 触发器的增量维护**

现在每帧遍历整个重叠集合做差分。真要优化的话，让宽相位直接吐出
"新增 / 消失"的对（它内部本来就知道），触发器只处理增量。
但这需要宽相位也维护跨帧的对集合，复杂度会上升 —— 当前规模下不划算。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| 扫掠停下的位置**永不穿透**（保守），宁可早停也不漏报 | `停下的位置总是保守的` |
| `ShapeCastHit::normal` 朝向扫掠形状来的一侧（与 `RaycastHit` 一致） | `球撞墙的接触位置可以手算` |
| 已经贴着表面、且位移不朝表面里去时**不算命中** | `角色 走在平地上`（这条是走路的前提） |
| 起点重叠时给出 `startPenetrating` + `depth`，沿 normal 推 depth 就脱开 | `起点就重叠时给出脱困信息` |
| 撞墙后沿墙面滑行，不是停下 | `贴着墙走不会卡住` |
| 台阶落点判据是"面朝上"，不是"能站人" | `能自动跨上台阶` |
| 陡坡上不允许靠滑动往上爬，但垂直墙面不受影响（否则贴墙跳不起来） | `缓坡能走上去，陡坡会滑下来` |
| 触发器对键无序：(a,b) 与 (b,a) 是同一对 | `对键是无序的` |
| 销毁碰撞体必须补发 Exit | `销毁碰撞体会补发 Exit` |
| 事件只入队、不直接回调（回调里增删物体是安全的） | `事件派发给监听器` |

---

## M9 — World 整合 + Demo（已完成）

实现状态：`include/pe/collision/Collider.h`、`include/pe/scene/PhysicsWorld.h`、
`src/scene/PhysicsWorld.cpp`、`demo/main.cpp`
测试：`tests/test_world.cpp`
累计 **212 用例 / 80186 断言全过**

**关掉了前面留下的两条 P0**：

- **CCD**（M8 局限 2）：接在积分位置这一步里，只对"位移超过自身包围球半径"的
  刚体做扫掠。实测 300 m/s 的球撞 20 厘米厚的墙，开着被拦住、关掉直接穿过去。
- **速度感知的 fat AABB**（M3 局限 1）：宽相位重建率从 94% 降到个位数。
  踩了个坑：把 AABB 扫掠好再传给 `Update()` 是**无效**的（下一帧的"已扫掠 AABB"
  同样朝前伸，永远装不进上一帧的壳）。必须把预测量作为单独参数传进去，
  判定用紧致 AABB、预测只加在壳上。

### 已知局限

| # | 局限 | 什么时候会露馅 | 优先级 |
|---|---|---|---|
| 1 | **成堆的物体睡不着**：A 睡了被 B 叫醒、B 睡了被 A 叫醒，来回拉锯 | 场景里有大量堆叠时，它们会一直参与求解，白白消耗。单个物体躺在静态地面上不受影响（静态体永远不"醒"） | **P0** |
| 2 | `ContactEvent::impulse` 恒为 0，`End` 事件不带句柄 | 想按撞击力度做音量/破坏判定时。冲量要等求解之后才知道，而事件是在求解**之前**产生的 | P1 |
| 3 | 角色的"替身刚体"要游戏侧自己维护（Demo 里那段） | 每个项目都要重抄一遍。可以把它收进一个 `CharacterBody` 辅助类 | P1 |
| 4 | 没有移动平台支持 | 站在电梯上时角色不会跟着走（M8 局限 7 的延续，现在有 World 了可以做） | P1 |
| 5 | CCD 只扫第一个碰撞体，且不管旋转 | 复合形状的高速物体。抛射物基本都是单个球，够用 | P2 |
| 6 | 求解顺序依赖宽相位哈希表的遍历顺序，跨进程不保证一致 | 要做联机回滚同步时（M7 局限 5 的延续） | P2 |
| 7 | `Step()` 里所有阶段都是全量遍历，没有脏标记 | 上万个静态体时，`SyncBroadPhase` 会白白遍历一遍全部静态碰撞体 | P1 |

### 升级方向

**(1) 求解器孤岛 —— 现在是最该做的一条**

按接触关系求连通分量，整组一起睡、一起醒。一次解决三个问题：
- 堆叠能睡着了（本节局限 1）
- 每个岛的迭代次数可以按复杂度自适应（M7 局限 1 的摇晃）
- 岛之间无数据依赖，是唯一干净的并行切分点

**(2) 接触事件带上冲量**

把 `ContactBegin` 的产生推迟到求解之后：求解器 `EndFrame` 时遍历
`Contacts()`，对本帧新增的对补上 `normalImpulse` 之和再入队。
`End` 事件的句柄同理 —— 缓存里存下句柄对而不只是键。

**(3) 静态碰撞体的脏标记**

`SyncBroadPhase` 现在每帧遍历所有碰撞体。静态体的世界位姿永远不变，
完全不需要每帧算。加一个"这一帧动过没有"的标记，或者干脆把静态体和动态体
分成两个列表（宽相位内部已经是这么分的，World 这一层照做即可）。

**(4) `CharacterBody` 辅助类**

把 Demo 里那段"运动学替身 + 每帧同步位姿与速度"收进引擎，
顺便把"角色站在哪个刚体上"记下来，移动平台（局限 4）就顺手做掉了。

**(5) ConvexHull**（M5 局限 1）

现在有 World 了，顶点池终于有归属。步骤见 M5 那一节。

### 这一层留下的、后续模块要遵守的约定

| 约定 | 守护它的测试 |
|---|---|
| `Solve()` 期间不得创建/销毁刚体（底层数组会扩容，求解器持有的指针会悬垂） | 事件在 Step 末尾派发，`事件回调里增删物体是安全的` |
| `RigidBody::position` 是**质心**，`GetBodyTransform()` 才是物体原点 | `多碰撞体的质量合成` 各 SECTION |
| 同一段真实时间在不同帧率下走出相同结果 | `固定步长让结果与帧率无关` |
| 累加器跑满 `maxSubSteps` 就丢弃余量（防死亡螺旋） | `大延迟不会引发死亡螺旋` |
| 宽相位 `Update` 的预测量必须与紧致 AABB 分开传 | `速度预测把宽相位重建率压下来` |
| 只在接触**新建立**时唤醒；一方醒着就必须叫醒另一方 | `箱堆在完整管线里也稳定` / `休眠会让物体退出模拟` |
| 触发器不生成约束、不挡射线 | `触发器事件` / `射线查询` 的 `触发器不挡子弹` |
| 渲染必须用 `GetInterpolatedTransform(handle, Alpha())` | `渲染插值` |

## 明确不打算做的
软体、布料、流体、车辆、破碎、多线程求解、网络回滚同步。
这些不是"以后再说"，而是超出这个引擎的定位 —— 真要用请换成成熟的商业引擎。
